<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                xmlns:tools="http://schemas.android.com/tools">

    <CheckBox
        android:id="@+id/list_item_bill_paid_check_box"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"/>

    <TextView
        android:id="@+id/list_item_bill_title_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/list_item_bill_paid_check_box"
        android:textStyle="bold"
        android:padding="4dp"
        tools:text="Bill Title"/>

    <TextView
        android:id="@+id/list_item_bill_date_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/list_item_bill_paid_check_box"
        android:layout_below="@id/list_item_bill_title_text_view"
        android:padding="4dp"
        tools:text="Bill Date"/>

    <TextView
        android:id="@+id/list_item_bill_amount_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:padding="4dp"
        tools:text="Bill Amount"
        android:layout_toLeftOf="@id/list_item_bill_paid_check_box"
        android:layout_below="@id/list_item_bill_date_text_view"
        />


</RelativeLayout>